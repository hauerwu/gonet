package example;
	
enum FOO { X = 17; };

message Head {
	required string cmd = 1;
}

message Para {
	required int32 id = 1;
	optional string name = 2;
}

message Test {
	required Head head = 1;
	required Para para = 2;
}